# OPERATOR ONLY: copy to /opt/ezra-runtime/env/ops-live.env and chmod 600.
# Never commit the real file. Requires valid venue credentials elsewhere (not in git).
#
# Uncomment to declare live-capable process env for the OPS daemon after
# `python3 scripts/server/enable_full_autonomy_active_live.py --runtime-root /opt/ezra-runtime`
# and after live_order_guard closure is proven on this host.

# EZRAS_FULL_AUTONOMY_MODE=FULL_AUTONOMY_ACTIVE
# NTE_EXECUTION_MODE=live
# NTE_LIVE_TRADING_ENABLED=true
# COINBASE_ENABLED=true
# COINBASE_EXECUTION_ENABLED=true
# NTE_PAPER_MODE=false
# NTE_DRY_RUN=false
