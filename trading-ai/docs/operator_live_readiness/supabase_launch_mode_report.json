{
  "schema": "supabase_launch_mode_report_v1",
  "generated_note": "Regenerate after exporting real env: this snapshot reflects automated preflight probe (no secrets).",
  "valid_modes": {
    "MODE_A_remote_sync": {
      "requires": [
        "SUPABASE_URL non-empty",
        "SUPABASE_KEY or SUPABASE_SERVICE_ROLE_KEY non-empty"
      ],
      "preflight_check": 9,
      "implication": "Remote Supabase sync path available for trade/event sync as implemented in repo."
    },
    "MODE_B_local_first": {
      "requires": [
        "LIVE_FIRST_20_LOCAL_FIRST_OK=true"
      ],
      "preflight_check": 9,
      "implication": "Operator explicitly accepts local-first truth for this run; no remote Supabase required for preflight PASS."
    }
  },
  "probe_environment_2026_04_19": {
    "SUPABASE_URL_set": false,
    "SUPABASE_KEY_or_SERVICE_ROLE_set": false,
    "LIVE_FIRST_20_LOCAL_FIRST_OK": false,
    "satisfied_mode": "NONE",
    "preflight_check_9_would_be": "FAIL until MODE_A or MODE_B is satisfied"
  },
  "blockers_when_unset": "Preflight check 9 is CRITICAL — FAIL stops overall preflight (critical_ids include 9)."
}
