[phases.setup]
nixPkgs = ["python39", "python39Packages.pip"]

[phases.install]
cmds = ["python3.9 -m pip install -r trading-ai/requirements.txt"]

[start]
cmd = "cd trading-ai && PYTHONPATH=src python3.9 -m trading_ai.shark.run_shark"
